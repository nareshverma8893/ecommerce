<h1>Listing products</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Content</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @order.each do |order| %>
      <tr>
        <td><%= lineitem.product.title %></td>
        <td><%= lineitem.product.price %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to "cancel", new_book_path %>